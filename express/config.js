var config = {
    host: "http://localhost:4200",
    thinky: {
        host: "10.254.0.208",
        port: "28015",
        db: "peas"
    },
    session: {
        secret: "This is not really a secret but whatever"
    }
};

module.exports = config;
